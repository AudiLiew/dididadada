---
layout: default
---
<header>
  <h1>{{ title }}</h1>
  <div class="post-meta">
    <p class="post-meta__date"><time datetime="{% if mod_date %}{{ mod_date }}{% else  %}{{ date }}{% endif %}">{% if mod_date %}{{ mod_date | dateDisplay }}{% else  %}{{ date | dateDisplay }}{% endif %}</time></p>
  </div>
</header>
<div class="post-content">
  {{ content | safe }}

  {# author card #}
  {# {% include "../components/author.njk" %} #}
</div>
{# Tags #}
{# {% if tags %}
  {% for tag in tags %}
    {% if tag != "posts" %}
    <a href="/{{ tag }}">{{ tag | capitalize }}</a>
    {% endif %}
  {% endfor %}
{% endif %} #}